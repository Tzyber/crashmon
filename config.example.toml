# crashmon Konfiguration (Beispiel)
# Kein "Journal-Pfade"-Knopf: Backend nutzt sd_journal mit Systempfaden
# (JournalFiles::All) — konfigurierbare Pfade waeren toter Zustand.

# Zielverzeichnis fuer JSON-Crash-Reports
dump_dir = "/var/lib/crashmon"

# Log-Level (trace|debug|info|warn|error)
log_level = "info"

# k4 (Retention): Report-Dateien begrenzen. Beide optional, None = unbegrenzt.
# max_reports: hoechstens so viele crash-*.json behalten (juengste gewinnen).
# max_age_days: Reports aelter als N Tage loeschen.
#max_reports = 200
#max_age_days = 90
